<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }
        .email-container {
            max-width: 600px;
            margin: 20px auto;
            background-color: #ffffff;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
        }
        .email-header {
            background: linear-gradient(135deg, #f4a261, #e76f51);
            color: white;
            padding: 30px;
            text-align: center;
        }
        .email-header h1 {
            margin: 0;
            font-size: 24px;
        }
        .email-header .paw-icon {
            font-size: 40px;
            margin-bottom: 10px;
        }
        .email-body {
            padding: 30px;
        }
        .info-section {
            background-color: #f8f9fa;
            border-left: 4px solid #f4a261;
            padding: 15px;
            margin-bottom: 20px;
            border-radius: 5px;
        }
        .info-section h2 {
            color: #e76f51;
            font-size: 18px;
            margin-top: 0;
            margin-bottom: 15px;
        }
        .info-row {
            margin-bottom: 10px;
        }
        .info-label {
            font-weight: bold;
            color: #555;
            display: inline-block;
            min-width: 100px;
        }
        .info-value {
            color: #333;
        }
        .message-section {
            background-color: #fff;
            border: 2px solid #f4a261;
            padding: 20px;
            margin-top: 20px;
            border-radius: 5px;
        }
        .message-section h2 {
            color: #e76f51;
            font-size: 18px;
            margin-top: 0;
            margin-bottom: 15px;
        }
        .message-content {
            background-color: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
            white-space: pre-wrap;
            word-wrap: break-word;
        }
        .email-footer {
            background-color: #2a2a2a;
            color: #fff;
            padding: 20px;
            text-align: center;
            font-size: 14px;
        }
        .email-footer a {
            color: #f4a261;
            text-decoration: none;
        }
        .reply-button {
            display: inline-block;
            background: linear-gradient(135deg, #f4a261, #e76f51);
            color: white;
            padding: 12px 30px;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 20px;
            font-weight: bold;
        }
        .association-badge {
            display: inline-block;
            background-color: #e76f51;
            color: white;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 14px;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="email-header">
            <div class="paw-icon">🐾</div>
            <h1>Nouveau message depuis HowIMetMyHuman</h1>
            <div class="association-badge">{{ association.name }}</div>
        </div>

        <div class="email-body">
            <p>Bonjour,</p>
            <p>Vous avez reçu un nouveau message via la plateforme <strong>HowIMetMyHuman</strong>.</p>

            <div class="info-section">
                <h2>📋 Informations du contact</h2>
                <div class="info-row">
                    <span class="info-label">Nom :</span>
                    <span class="info-value">{{ name }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Email :</span>
                    <span class="info-value"><a href="mailto:{{ email }}">{{ email }}</a></span>
                </div>
                {% if phone %}
                <div class="info-row">
                    <span class="info-label">Téléphone :</span>
                    <span class="info-value">{{ phone }}</span>
                </div>
                {% endif %}
                <div class="info-row">
                    <span class="info-label">Sujet :</span>
                    <span class="info-value">{{ subject }}</span>
                </div>
            </div>

            <div class="message-section">
                <h2>💬 Message</h2>
                <div class="message-content">{{ message }}</div>
            </div>

            <div style="text-align: center;">
                <a href="mailto:{{ email }}?subject=Re: {{ subject|url_encode }}" class="reply-button">
                    Répondre à {{ name }}
                </a>
            </div>

            <p style="margin-top: 30px; color: #666; font-size: 14px;">
                <strong>💡 Conseil :</strong> Pensez à répondre dans les 48h pour offrir une meilleure expérience aux adoptants potentiels.
            </p>
        </div>

        <div class="email-footer">
            <p><strong>HowIMetMyHuman</strong> - Plateforme de mise en relation adoptants / associations</p>
            <p>Cet email a été envoyé automatiquement, merci de ne pas y répondre directement.</p>
            <p>Pour répondre au contact, utilisez le bouton "Répondre" ci-dessus.</p>
        </div>
    </div>
</body>
</html>
